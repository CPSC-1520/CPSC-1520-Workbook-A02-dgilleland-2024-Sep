alert('I am about to load the body of this page.');
// JavaScript can "block" rendering. The line above is an
// example of this. This script is loaded and executed
// immediately by the browser because the script is loaded
// in the <head> part of the HTML.
// Because we're now waiting for the user to close the
// alert box, the browser is prevented from rendering the
// <body>.